---
summary: "重命名为 Coco 的分歧点记录（已确认）"
status: "resolved"
slug: "rename-to-coco"
created_at: "2026-01-20"
---

# 分歧点 / 结论

本次变更已确认无分歧，按以下决策执行：

- 仅修改本项目内出现的名字（不涉及外部系统）。
- 对外品牌名使用 `Coco`；代码/路径等使用 `coco`（按上下文映射大小写）。
- 不提供兼容层：不保留旧命令/旧包名/旧落盘目录/旧环境变量。
- 顶层目录（本机仓库文件夹名）不改。
- OpenSpec：spec-id 与内容均同步改名。
