每个任务执行 agent 分配一个任务工作流(todo list), 在其交付任务但是未主动更新todo list时对它试探提醒,更新 todo, 如果更新了则提示下一步 / 状态转移
任务的每个节点提问有无分歧需要 lead 确认, lead 能否确认, 他是否理解了用户意图, 或者提供options, 转交决策权给用户, 合理管理此处上下文, 最终可能只是在 subagent 执行的环节增加一条用户输入, 向subagent提问有无分歧需要 lead 确认以及lead的确认, 用户的决策,都将在已产生上下文中被手动取消